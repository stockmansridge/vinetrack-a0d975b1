              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
C              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
@              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
@              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
P              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
U              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
@              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
G              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
@              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
G              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
G              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
Z              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
@              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
G              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
L              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
G              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
C              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
@              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
C              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
*              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
*              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
*              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
T              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
H              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
V              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
T              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
k              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
W              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
k              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
*              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
*              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
U              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
A              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
D              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
A              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
*              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
—              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
*              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
—              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
'              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
*              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
`              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
P              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
`              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
*              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
G              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
L              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
:              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
:              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
G              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
C              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
?              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
:              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
4              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
P              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
U              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
B              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
B              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
|              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
|              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
K              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
L              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
P              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
?              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
?              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
L              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
?              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
:              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
:              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
k              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
C              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
4              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
4              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
:              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
:              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
_              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
3              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
4              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
9              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
:              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
2              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
2              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
6              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
6              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
k              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
j              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
+              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
3              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
3              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
3              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
k              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
2              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
8              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
5              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
k              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
1              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
I              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
&              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
?              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
j              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
3              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
3              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
,              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
z              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
[              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
3              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
8              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
0              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
]              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
x              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
w              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
f              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
b              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
j              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
-              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
"              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
v              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
:              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
(              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
G              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
u              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
K              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
K              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
y              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
!              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
t              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
p              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
.              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
h              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
n              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
c              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
s              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
N              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
=              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
{              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
m              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
g              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
S              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
e              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
C              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
a              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
r              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
d              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
i              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
<              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
/              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
o              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
l              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
>              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
               {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
)              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
;              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
}              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>

              {hasImage &&
                (urls.length > 0 ? (
                  <div
                    data-guide-step-image={step.id}
                    className={cn(
                      "grid w-full gap-2 overflow-hidden rounded-xl border border-border bg-muted/30 p-3",
                      urls.length > 1 && "sm:grid-cols-2",
                      imageSide,
                    )}
                  >
                    {urls.map((u, n) => (
                      <GuideImageZoom
                        key={u}
                        src={u}
                        alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                        className="flex items-center justify-center"
                      >
                        <img
                          src={u}
                          alt={urls.length > 1 ? `${step.heading} (${n + 1})` : step.heading}
                          loading="lazy"
                          decoding="async"
                          className={cn(
                            "w-auto max-w-full object-contain transition-transform group-hover:scale-[1.01]",
                            urls.length > 1 ? "max-h-[220px]" : "max-h-[380px]",
                          )}
                        />
                      </GuideImageZoom>
                    ))}
                  </div>
                ) : (
                  <GuideScreenshot
                    imageKey={step.imageKey!}
                    alt={step.heading}
                    className={imageSide}
                  />
                ))}
            </Card>
